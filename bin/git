#!/usr/bin/env python
"""" git --- ovis supporter
@Author: dbd
@Version: 0.0.1
"""
""" Commentary:
"""

""" Code: """
import os
import sys
import subprocess as sp

local_path = os.path.dirname(os.path.realpath(__file__))

exec(open(os.path.join(local_path, 'ovis'), 'rb').read())


which_cmd = 'utils_which'
git_bin = utils[which_cmd]('git')

def git(*args, **kwargs):
    print args
    print kwargs
